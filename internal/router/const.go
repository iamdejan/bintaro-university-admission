package router

const (
	cookieNameErrorMessage = "__Host-error_message"
	cookieNameSessionToken = "__Host-session_token" //nolint:gosec // this is only cookie-name, not the value
)

const (
	logKeyError       = "error"
	logKeyEmail       = "email"
	logKeyUserID      = "user_id"
	logKeySessionType = "session_type"
)
